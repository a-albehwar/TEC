import * as React from 'react';
//import styles from './CompetitionPopup.module.scss';
import { ICompetitionPopupProps } from './ICompetitionPopupProps';
import { escape } from '@microsoft/sp-lodash-subset';
import * as moment from 'moment';
import { Modal } from 'react-bootstrap';

export default class CompetitionPopup extends React.Component<ICompetitionPopupProps, {}> {
  public render(): React.ReactElement<ICompetitionPopupProps> {
    return (
      <div>
        
      </div>
    );
  }
}
